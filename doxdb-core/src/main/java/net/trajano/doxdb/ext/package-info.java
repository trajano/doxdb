/**
 * This package provides the Extension API. The extension API allows the
 * creation of application specific configuration while maintaining the core.
 * The extension points are done through interfaces which are implemented as
 * EJBs in another EJB jar.
 */
package net.trajano.doxdb.ext;
